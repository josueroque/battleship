import { Typography } from "@mui/material";
import React from "react";

const Settings = () => {
  return (
    <div>
      <Typography variant='h3'>Configura el Juego </Typography>
      <Typography variant='h5'>Selecciona el nivel de dificultad</Typography>
    </div>
  );
};

export default Settings;
